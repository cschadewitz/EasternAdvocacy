# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    title:
        label: Title
        type: text
    subtitle:
        label: Subtitle
        type: text
    description:
        label: Desciption
        type: text
    template:
        label: Mail Template
        relation: template
        select: subject
    require_user:
        label: Require user
        type: switch
    is_active:
        label: Is active?
        type: switch